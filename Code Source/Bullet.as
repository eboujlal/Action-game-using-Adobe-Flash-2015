package  {	import flash.display.MovieClip;	import flash.events.Event;	import flash.display.Stage;		public class Bullet extends MovieClip {				private var speed:int = 30;		private var initialX:int;				public function Bullet(playerX:int, playerY:int, playerDirection:String) {						// constructor code			if(playerDirection == "left") {				speed = -30 ; //Bullet Speed				x = playerX - 25;			} else if(playerDirection == "right") {				speed = 30;				x = playerX + 25;			}
			else if(playerDirection == "walo") {
				removeSelf();
			}			y = playerY - 84 ;						initialX = x; //conserver x de bullet						addEventListener(Event.ENTER_FRAME, loop);		}				public function loop(e:Event):void		{			//loop function boucle			x += speed;						if(speed > 0) { //si player marche a droite				if(x > initialX + 300) { //supp bullet apres 300px					removeSelf(); //remove it				}			} else { //esi player marche a gauche				if(x < initialX - 300) {  //supp bullet apres 300px					removeSelf(); //remove it				}			}		}				public function removeSelf():void		{			trace("The Bullet removed");			removeEventListener(Event.ENTER_FRAME, loop); //stop the loop			this.parent.removeChild(this);// supp un enmy determiner mo7addad					}	}	}